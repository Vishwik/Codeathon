import { Activity, CheckCircle2, Command, Download, FileText, LayoutDashboard, Radio, Scale, Search, ShieldAlert, ShieldCheck, Terminal, X } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { API_BASE_URL, apiClient, ApiError } from "./api/client";
import type { Assessment, AuditEvent, Decision, Health, ScoreInput, Summary, TransactionType } from "./types";

const nav = [["overview","Overview",LayoutDashboard],["investigations","Investigations",ShieldAlert],["live","Live Stream",Radio],["demo","Demo Console",Terminal],["model","Model & Rules",Scale],["audit","Audit Trail",FileText]] as const;
const blankSummary: Summary = { total_transactions: 0, suspicious_count: 0, high_risk_count: 0, medium_risk_count: 0, low_risk_count: 0, avg_risk_score: 0 };

export default function App() {
 const [screen,setScreen] = useState("overview"); const [health,setHealth] = useState<Health | null>(null); const [records,setRecords] = useState<Assessment[]>([]); const [summary,setSummary] = useState<Summary | null>(null); const [selected,setSelected] = useState<Assessment | null>(null); const [palette,setPalette] = useState(false); const [exportOpen,setExportOpen] = useState(false); const [notice,setNotice] = useState<string | null>(null); const [busy,setBusy] = useState(false); const [audits,setAudits] = useState<AuditEvent[]>([]);
 const refresh = async () => { const nextHealth = await apiClient.health(); setHealth(nextHealth); if (!nextHealth.online) { setRecords([]); setSummary(null); return; } try { const [transactions,nextSummary] = await Promise.all([apiClient.transactions(),apiClient.summary()]); setRecords(transactions); setSummary(nextSummary); } catch (error) { setNotice(error instanceof Error ? error.message : "Unable to load backend records"); } };
 useEffect(() => { void refresh(); const timer = window.setInterval(() => void refresh(), 15000); return () => window.clearInterval(timer); }, []);
 useEffect(() => { const listener = (event: KeyboardEvent) => { if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "k") { event.preventDefault(); setPalette(true); } if (event.key === "Escape") setPalette(false); }; window.addEventListener("keydown", listener); return () => window.removeEventListener("keydown", listener); }, []);
 const derived = summary ?? (records.length ? { ...blankSummary, total_transactions: records.length, suspicious_count: records.filter((item) => item.suspicious).length, high_risk_count: records.filter((item) => item.risk_level === "HIGH").length, medium_risk_count: records.filter((item) => item.risk_level === "MEDIUM").length, low_risk_count: records.filter((item) => item.risk_level === "LOW").length, avg_risk_score: records.reduce((total,item) => total + item.risk_score, 0) / records.length } : null);
 const decide = async (decision: Decision, note: string) => { if (!selected) return; setBusy(true); try { const updated = await apiClient.decision(selected.transaction_id, decision, note); setSelected(updated); setAudits((current) => [{ id: crypto.randomUUID(), timestamp: new Date().toISOString(), transactionId: selected.transaction_id, decision, note }, ...current]); await refresh(); setNotice("Decision recorded by backend."); } catch (error) { setNotice(error instanceof Error ? error.message : "Backend decision failed"); } finally { setBusy(false); } };
 const score = async (input: ScoreInput) => { setBusy(true); try { const result = await apiClient.score(input); setRecords((current) => [result, ...current.filter((item) => item.transaction_id !== result.transaction_id)]); setSelected(result); setScreen("investigations"); setNotice(result.fallback_used ? "FALLBACK MODE • conservative backend result" : "Backend assessment received."); } catch (error) { setNotice(error instanceof Error ? error.message : "BACKEND OFFLINE • ML UNAVAILABLE"); } finally { setBusy(false); } };
 return <div className="app-shell"><header className="topbar"><div className="brand"><span><ShieldCheck size={23}/></span><div><strong>RiskOps AI</strong><small>Transaction Risk & Investigation</small></div></div><button className="search-launch" onClick={() => setPalette(true)}><Command size={15}/> Search transactions <kbd>Ctrl K</kbd></button><div className={"health " + (health?.online ? "online":"offline")}><Activity size={14}/>{health?.online ? "BACKEND ONLINE":"BACKEND OFFLINE • ML UNAVAILABLE"}{health?.online && health.latencyMs !== null && <small>{health.latencyMs} ms</small>}{health?.fallbackUsed && <b>FALLBACK MODE</b>}</div></header>
 <aside className="sidebar"><nav><p>Investigative Operations</p>{nav.map(([id,label,Icon]) => <button className={screen===id?"active":""} onClick={() => setScreen(id)} key={id}><Icon size={17}/><span>{label}</span>{id==="investigations" && records.filter((item)=>item.suspicious && !item.decision).length>0 && <b>{records.filter((item)=>item.suspicious && !item.decision).length}</b>}</button>)}</nav><div className="engine-card"><p>Active ML Components</p><span>XGBoost Classifier</span><span>Isolation Forest</span><span>Hybrid Risk Fusion</span><span>Native TreeSHAP</span></div></aside>
 <main>{screen==="overview" && <Overview summary={derived} records={records} health={health} onSelect={setSelected} onExport={() => setExportOpen(true)}/>} {screen==="investigations" && <Investigations records={records} onSelect={setSelected}/>} {screen==="live" && <Live health={health} records={records} onRefresh={refresh} onSelect={setSelected}/>} {screen==="demo" && <Demo busy={busy} onScore={score}/>} {screen==="model" && <Model/>} {screen==="audit" && <Audit events={audits}/>}</main>
 {selected && <Drawer record={selected} busy={busy} onClose={() => setSelected(null)} onDecision={decide}/>} {palette && <Palette records={records} onClose={() => setPalette(false)} onSelect={(item) => { setSelected(item); setPalette(false); }} onNavigate={(target) => { setScreen(target); setPalette(false); }}/>} {exportOpen && <Export records={records} onClose={() => setExportOpen(false)}/>} {notice && <button className="notice" onClick={() => setNotice(null)}>{notice}<X size={15}/></button>}</div>;
}
function Overview({summary,records,health,onSelect,onExport}:{summary:Summary|null;records:Assessment[];health:Health|null;onSelect:(x:Assessment)=>void;onExport:()=>void}) { return <section className="view"><div className="view-head"><div><p>Risk command center</p><h1>Overview</h1></div><button className="primary" onClick={onExport}><Download size={16}/> Export records</button></div>{!health?.online?<Offline/>:<><div className="stat-grid">{[["Transactions",summary?.total_transactions],["Suspicious",summary?.suspicious_count],["High risk",summary?.high_risk_count],["Average risk",summary?.avg_risk_score?.toFixed(1)]].map(([label,value])=><article key={String(label)}><small>{label}</small><strong>{value ?? "—"}</strong></article>)}</div><section className="panel"><div className="panel-head"><h2>Recent backend assessments</h2><span>{records.length} loaded</span></div><RecordTable records={records} onSelect={onSelect}/></section></>}</section>; }
function Investigations({records,onSelect}:{records:Assessment[];onSelect:(x:Assessment)=>void}) { const queue=records.filter((item)=>item.suspicious); return <section className="view"><div className="view-head"><div><p>Human-in-the-loop queue</p><h1>Investigations</h1></div></div><section className="panel"><div className="panel-head"><h2>Suspicious transactions</h2><span>{queue.length} records</span></div><RecordTable records={queue} onSelect={onSelect}/></section></section>; }
function Live({health,records,onRefresh,onSelect}:{health:Health|null;records:Assessment[];onRefresh:()=>Promise<void>;onSelect:(x:Assessment)=>void}) { return <section className="view"><div className="view-head"><div><p>Backend transaction feed</p><h1>Live Stream</h1></div><button className="secondary" onClick={() => void onRefresh()}>Refresh</button></div>{!health?.online?<Offline/>:<section className="panel"><p className="muted">Polls the backend every 15 seconds. This client does not generate transactions or scores.</p><RecordTable records={records} onSelect={onSelect}/></section>}</section>; }
function Demo({busy,onScore}:{busy:boolean;onScore:(input:ScoreInput)=>Promise<void>}) { const [input,setInput]=useState<ScoreInput>({transaction_id:"txn_demo_001",step:120,type:"TRANSFER",amount:48500,sender_id:"C123",recipient_id:"C456"}); const change=(key:keyof ScoreInput,value:string)=>setInput((current)=>({...current,[key]:key==="amount"||key==="step"?Number(value):value} as ScoreInput)); const preset=(kind:"normal"|"mule"|"velocity")=>{ const next={normal:{type:"PAYMENT",step:120,amount:125.5,sender_id:"C109",recipient_id:"M382"},mule:{type:"TRANSFER",step:142,amount:48500,sender_id:"C123",recipient_id:"C456"},velocity:{type:"CASH_OUT",step:210,amount:95000,sender_id:"C391",recipient_id:"M782"}}[kind]; setInput({transaction_id:"txn_"+kind+"_"+Date.now(),...next} as ScoreInput); }; return <section className="view"><div className="view-head"><div><p>Real API scoring only</p><h1>Demo Console</h1></div></div><section className="panel demo"><p>Select a scenario, then send only the approved six-field payload to <code>POST /api/v1/transactions/score</code>. No balance fields, local scoring, or generated fallback scores are used.</p><div className="preset-row"><button onClick={()=>preset("normal")}>Normal transaction</button><button onClick={()=>preset("mule")}>Mule transfer</button><button onClick={()=>preset("velocity")}>Novel anomaly</button></div><div className="form-grid"><label>Transaction ID<input value={input.transaction_id} onChange={(e)=>change("transaction_id",e.target.value)}/></label><label>Step<input type="number" value={input.step} onChange={(e)=>change("step",e.target.value)}/></label><label>Type<select value={input.type} onChange={(e)=>change("type",e.target.value)}>{["CASH_IN","CASH_OUT","DEBIT","PAYMENT","TRANSFER"].map((type)=><option key={type}>{type}</option>)}</select></label><label>Amount<input type="number" min="0.01" value={input.amount} onChange={(e)=>change("amount",e.target.value)}/></label><label>Sender ID<input value={input.sender_id} onChange={(e)=>change("sender_id",e.target.value)}/></label><label>Recipient ID<input value={input.recipient_id} onChange={(e)=>change("recipient_id",e.target.value)}/></label></div><button className="primary" disabled={busy} onClick={()=>void onScore(input)}>{busy?"Requesting backend…":"Score with RiskEngine API"}</button></section></section>; }
function Model(){return <section className="view"><div className="view-head"><div><p>Backend-owned model configuration</p><h1>Model & Rules</h1></div></div><section className="panel model"><article><h2>XGBoost Classifier</h2><p>Produces calibrated fraud probability and native TreeSHAP classifier explanations.</p></article><article><h2>Isolation Forest</h2><p>Produces anomaly percentile and severity against a fixed legitimate calibration reference.</p></article><article><h2>Hybrid Risk Fusion</h2><p>Combines backend probability and anomaly severity into a 0–100 review-priority score.</p></article><article><h2>Policy</h2><p>LOW below 30, MEDIUM from 30 to below 70, HIGH from 70. Rules and thresholds are versioned with the backend artifact bundle.</p></article></section><p className="extension-note">No Redis, autoencoder, Bayesian fusion, device intelligence, IP intelligence, account graph, or automated blocking is presented as active capability.</p></section>;}
function Audit({events}:{events:AuditEvent[]}){return <section className="view"><div className="view-head"><div><p>Local session view of successful backend decisions</p><h1>Audit Trail</h1></div></div><section className="panel">{events.length?events.map((event)=><article className="audit" key={event.id}><b>{event.decision}</b><span>{event.transactionId}</span><small>{event.timestamp}{event.note?" • "+event.note:""}</small></article>):<p className="empty-copy">No decisions have been recorded in this session. The permanent audit trail is backend-owned.</p>}</section></section>;}
function RecordTable({records,onSelect}:{records:Assessment[];onSelect:(x:Assessment)=>void}){return records.length?<div className="table">{records.map((item)=><button className="record" key={item.transaction_id} onClick={()=>onSelect(item)}><span><b>{item.transaction_id}</b><small>{item.type} • {item.sender_id} → {item.recipient_id}</small></span><span>{item.amount.toLocaleString()} units</span><span>{(item.fraud_probability*100).toFixed(1)}%</span><strong className={"risk-"+item.risk_level.toLowerCase()}>{item.risk_score.toFixed(1)} {item.risk_level}</strong></button>)}</div>:<p className="empty-copy">No backend transactions are available.</p>;}
function Drawer({record,busy,onClose,onDecision}:{record:Assessment;busy:boolean;onClose:()=>void;onDecision:(x:Decision,note:string)=>void}){const[note,setNote]=useState("");const rows=[...record.explanation.contributions].sort((a,b)=>Math.abs(b.contribution)-Math.abs(a.contribution));return <div className="drawer-backdrop" onMouseDown={onClose}><aside className="drawer" onMouseDown={(e)=>e.stopPropagation()}><header><div><p>Investigation</p><h2>{record.transaction_id}</h2></div><button onClick={onClose}><X/></button></header>{record.fallback_used&&<div className="fallback">FALLBACK MODE • conservative backend fallback</div>}<div className="metric-grid">{[["Fraud probability",(record.fraud_probability*100).toFixed(1)+"%"],["Anomaly severity",record.anomaly_score.toFixed(3)],["Hybrid risk",record.risk_score.toFixed(1)+" / 100"],["Risk level",record.risk_level]].map(([label,value])=><article key={label}><small>{label}</small><strong>{value}</strong></article>)}</div><section><h3>Assessment evidence</h3><ul>{record.reasons.map((reason)=><li key={reason.code}><b>{reason.source}</b>{reason.message}</li>)}</ul></section><section><h3>Classifier explanation • Native TreeSHAP</h3><p className="muted">Signed raw log-odds contributions for the XGBoost classifier; not probability points or hybrid-risk points.</p>{rows.length?rows.map((row)=><div className="shap" key={row.feature}><span>{row.label}<small>{row.value.toFixed(4)}</small></span><i className={row.contribution>=0?"plus":"minus"} style={{width:Math.min(Math.abs(row.contribution)*35,50)+"%"}}/><b>{row.contribution>=0?"+":""}{row.contribution.toFixed(3)}</b></div>):<p className="empty-copy">No TreeSHAP values were returned.</p>}</section><section className="context"><h3>Transaction context</h3><p>{record.type} • {record.amount.toLocaleString()} units • Step {record.step}</p><p>{record.sender_id} → {record.recipient_id}</p></section><footer><textarea value={note} onChange={(e)=>setNote(e.target.value)} placeholder="Investigator note (stored by backend)"/><button disabled={busy} onClick={()=>onDecision("APPROVE",note)}><CheckCircle2 size={16}/> Approve</button><button className="block" disabled={busy} onClick={()=>onDecision("BLOCK",note)}><ShieldAlert size={16}/> Block</button></footer></aside></div>;}
function Palette({records,onClose,onSelect,onNavigate}:{records:Assessment[];onClose:()=>void;onSelect:(x:Assessment)=>void;onNavigate:(x:string)=>void}){const[q,setQ]=useState("");const results=useMemo(()=>records.filter((r)=>[r.transaction_id,r.sender_id,r.recipient_id,r.type].join(" ").toLowerCase().includes(q.toLowerCase())).slice(0,8),[q,records]);return <div className="modal-backdrop" onMouseDown={onClose}><section className="palette" onMouseDown={(e)=>e.stopPropagation()}><div><Search size={18}/><input autoFocus value={q} onChange={(e)=>setQ(e.target.value)} placeholder="Search backend transactions"/><button onClick={onClose}><X/></button></div><p>Views</p>{[["overview","Overview"],["investigations","Investigations"],["demo","Demo Console"]].map(([id,label])=><button key={id} onClick={()=>onNavigate(id)}>{label}</button>)}<p>Transactions</p>{results.length?results.map((r)=><button key={r.transaction_id} onClick={()=>onSelect(r)}>{r.transaction_id}<small>{r.type} • {r.risk_score.toFixed(1)} {r.risk_level}</small></button>):<em>No matching backend records.</em>}</section></div>;}
function Export({records,onClose}:{records:Assessment[];onClose:()=>void}){const download=()=>{const blob=new Blob([JSON.stringify({exported_at:new Date().toISOString(),transactions:records},null,2)],{type:"application/json"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download="riskops-transactions.json";a.click();URL.revokeObjectURL(url);};return <div className="modal-backdrop"><section className="export"><button onClick={onClose}><X/></button><Download size={23}/><h2>Export backend transaction records</h2><p>Exports exactly the records returned by the backend, without client-generated scores.</p><button className="primary" onClick={download}>Download JSON ({records.length})</button></section></div>;}
function Offline(){return <section className="offline"><ShieldAlert size={24}/><h2>BACKEND OFFLINE • ML UNAVAILABLE</h2><p>RiskOps does not generate replacement fraud, anomaly, or risk scores in the browser. Start FastAPI at {API_BASE_URL} to load live assessments.</p></section>; }
